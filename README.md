# dy_job_quick_chat

![GitHub Release](https://img.shields.io/github/v/release/DyroS3/dy_job_quick_chat?label=版本&color=blue)
![GitHub License](https://img.shields.io/github/license/DyroS3/dy_job_quick_chat?label=许可证&color=green)
![FiveM](https://img.shields.io/badge/FiveM-ESX-orange.svg)
![GitHub Actions Workflow Status](https://img.shields.io/github/actions/workflow/status/DyroS3/dy_job_quick_chat/release.yml?label=构建)

## 📖 简介

FiveM 职业快捷语录系统，根据玩家当前职业自动加载对应的话术模板，支持一键发送常用对话。专为角色扮演服务器设计，提升玩家沉浸式体验。

## ✨ 功能特性

- **职业自动识别** - 自动检测玩家当前职业，加载对应话术
- **分类话术管理** - 按场景分类组织语录，快速定位
- **动态占位符** - 支持 `{name}` 等占位符，点击后弹出表单填写动态内容
- **FiveM 颜色代码** - 支持 `^0` 到 `^9` 颜色代码解析与预览
- **实时搜索过滤** - 支持 Ctrl+F 快捷搜索，即时过滤
- **收藏夹功能** - 常用语录一键收藏，优先显示
- **深色/浅色主题** - 双主题切换，适应不同偏好
- **流畅动画效果** - GSAP 驱动的丝滑交互动画
- **热键快速呼出** - 可自定义热键，快速打开面板
- **可折叠侧边栏** - 紧凑模式与展开模式自由切换

## 📸 截图预览

![演示](demo.webp)

## 🚀 快速开始

### 依赖要求

| 依赖 | 版本 | 必需 |
|------|------|------|
| FiveM 服务器 | 最新版 | ✅ |
| es_extended (ESX Legacy) | v1.9.0+ | ✅ |
| oxmysql | v2.0.0+ | ❌ 可选 |

### 安装步骤

1. **下载资源**
   ```bash
   # 从 Releases 下载最新版本
   # 或克隆仓库后自行构建
   git clone https://github.com/DyroS3/dy_job_quick_chat.git
   cd dy_job_quick_chat/web
   npm install
   npm run build
   ```

2. **放入资源目录**
   ```
   将 dy_job_quick_chat 文件夹放入 resources/[local]/ 目录
   ```

3. **添加启动配置**
   ```cfg
   # server.cfg
   ensure dy_job_quick_chat
   ```

4. **重启服务器**
   ```
   重启 FiveM 服务器或执行 refresh && ensure dy_job_quick_chat
   ```

## ⚙️ 配置指南

### 基础配置 (shared/config.lua)

```lua
Config = {}

-- 热键设置
Config.OpenKey = 'F6'
Config.OpenKeyDescription = '打开快捷语录'

-- 界面
Config.DefaultTheme = 'dark'     -- 'dark' | 'light'
Config.ShowCurrentJobOnly = true -- true: 只显示当前职业话术, false: 显示所有职业

-- 安全
Config.Security = {
    EnableCooldown = true, -- 消息冷却
    CooldownTime = 1000,   -- 冷却时间 (ms)
    ValidateQuotes = true, -- 验证话术来源
    LogSuspicious = true,  -- 记录可疑行为
}

-- 调试
Config.Debug = false
```

### 职业话术配置

职业话术文件位于 `jobs/` 目录，每个职业一个文件：

```lua
-- jobs/police.lua
JobQuotes['police'] = {
    id = 'police',
    name = '警察',
    icon = 'mdi:police-badge',
    description = '执法人员常用语录',
    categories = {
        {
            id = 'patrol',
            name = '日常巡逻',
            expanded = true,
            quotes = {
                { id = 'p001', text = '^1[警察]^0 请靠边停车，接受检查', favorite = false },
                { id = 'p002', text = '^4[LSPD]^0 请出示您的驾照和行驶证', favorite = false },
            }
        },
        {
            id = 'judgment',
            name = '判罚公告',
            expanded = true,
            quotes = {
                -- 带占位符的话术示例
                {
                    id = 'j001',
                    text = '^3嫌疑人{name}因^8{crime}^3。罚款{fine}，监狱{time}。',
                    favorite = false,
                    placeholders = {
                        { key = 'name', label = '嫌疑人名称', required = true, maxLength = 20 },
                        { key = 'crime', label = '罪名', default = '违法行为', required = true, maxLength = 30 },
                        { key = 'fine', label = '罚款金额', default = '10W', maxLength = 10 },
                        { key = 'time', label = '监狱时间', default = '10分钟', maxLength = 10 }
                    }
                }
            }
        }
    }
}
```

### 占位符配置

话术支持动态占位符，点击后会弹出表单让玩家填写：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `key` | string | ✅ | 占位符标识，对应 `{key}` |
| `label` | string | ✅ | 表单显示的中文标签 |
| `default` | string | ❌ | 默认值 |
| `required` | boolean | ❌ | 是否必填，默认 false |
| `maxLength` | number | ❌ | 最大长度，默认 100 |

### FiveM 颜色代码

话术文本支持 FiveM 原生颜色代码，UI 会自动解析并预览颜色效果：

| 代码 | 颜色 | HEX |
|------|------|-----|
| `^0` | 白色 (默认) | 自适应主题 |
| `^1` | 红色 | #F44336 |
| `^2` | 绿色 | #4CAF50 |
| `^3` | 黄色 | #FFEB3B |
| `^4` | 蓝色 | #42A5F5 |
| `^5` | 浅蓝 | #03A9F4 |
| `^6` | 紫色 | #9C27B0 |
| `^7` | 白色 (默认) | 自适应主题 |
| `^8` | 橙色 | #FF5722 |
| `^9` | 灰色 | #9E9E9E |

**示例**: `^1[警察]^0 请靠边停车` 会显示为 <span style="color:#F44336">[警察]</span> 请靠边停车

## 🎮 使用说明

### 命令与热键

| 操作 | 按键/命令 | 描述 |
|------|-----------|------|
| 打开/关闭面板 | `F6` | 可在配置中自定义 |
| 关闭面板 | `ESC` | 随时关闭 |
| 搜索语录 | `Ctrl + F` | 聚焦搜索框 |
| 重载配置 | `/quickchat reload` | 管理员命令 |

### 界面操作

1. **选择职业** - 点击左侧职业图标切换
2. **展开分类** - 点击分类名称展开/收起
3. **发送语录** - 点击语录项直接发送
4. **收藏语录** - 右键点击语录添加收藏
5. **搜索过滤** - 在搜索框输入关键词过滤
6. **切换主题** - 点击底部太阳/月亮图标

## 🛠️ 开发者指南

### 项目结构

```
dy_job_quick_chat/
├── client/                 # 客户端 Lua 脚本
│   └── main.lua
├── server/                 # 服务端 Lua 脚本
│   └── main.lua
├── shared/                 # 共享配置
│   └── config.lua
├── jobs/                   # 职业话术配置
│   ├── police.lua
│   ├── ambulance.lua
│   └── mechanic.lua
├── web/                    # NUI 前端 (Vue 3)
│   ├── src/
│   │   ├── components/     # Vue 组件
│   │   ├── composables/    # 组合式函数
│   │   ├── utils/          # 工具函数
│   │   ├── App.vue         # 主应用
│   │   └── main.ts         # 入口文件
│   └── package.json
├── .github/                # GitHub 模板
├── fxmanifest.lua          # 资源清单
├── README.md
├── CONTRIBUTING.md
├── LICENSE
└── CHANGELOG.md
```

### 本地开发

```bash
# 进入前端目录
cd web

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 浏览器访问 http://localhost:5173
```

### 构建部署

```bash
# 构建生产版本
npm run build

# 输出目录: web/dist/
# 将整个资源文件夹放入 FiveM 服务器即可
```

### 技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | Vue 3 + TypeScript |
| 构建工具 | Vite 5 |
| 样式方案 | 原生 CSS 变量 |
| 图标库 | Iconify |
| 后端框架 | FiveM Lua + ESX |

## 📝 更新日志

查看 [CHANGELOG.md](./CHANGELOG.md) 了解版本更新历史。

## 📄 许可证

本项目基于 [MIT 许可证](./LICENSE) 开源。

---

**Made with ❤️ for FiveM RP Community**
